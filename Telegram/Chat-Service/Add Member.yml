info:
  name: Add Member
  type: http
  seq: 6

http:
  method: POST
  url: "{{url}}/members"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  params:
    - name: cursor
      value: ""
      type: query
      disabled: true
    - name: limit
      value: ""
      type: query
      disabled: true
  body:
    type: json
    data: |-
      {
        "userIds": [
          "string"
        ]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
