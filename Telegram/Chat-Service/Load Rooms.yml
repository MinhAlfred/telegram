info:
  name: Load Rooms
  type: http
  seq: 7

http:
  method: GET
  url: "{{url}}/rooms"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  params:
    - name: cursor
      value: ""
      type: query
      disabled: true
    - name: limit
      value: ""
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
