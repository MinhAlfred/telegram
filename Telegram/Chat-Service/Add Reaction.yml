info:
  name: Add Reaction
  type: http
  seq: 5

http:
  method: POST
  url: "{{url}}/messages/3e85d9f1-993a-448f-8ff2-1f89fa2b005c/reactions"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  body:
    type: json
    data: |-
      {
        "emoji": "mặt ngu"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
