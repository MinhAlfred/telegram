info:
  name: Register
  type: http
  seq: 1

http:
  method: POST
  url: "{{url}}/users/auth/register"
  body:
    type: json
    data: |-
      {
        "username": "huy",
        "email": "huy@example.com",
        "password": "string",
        "displayName": "Huy Pro"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
