info:
  name: Login
  type: http
  seq: 2

http:
  method: POST
  url: "{{url}}/users/auth/login"
  body:
    type: json
    data: |-
      {
        "username": "lan",
        "password": "string"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
