info:
  name: Login
  type: http
  seq: 2

http:
  method: POST
  url: "{{url}}/users/auth/login"
  headers:
    - name: ""
      value: ""
  body:
    type: json
    data: |-
      {
        "username": "han",
        "password": "string"
      }
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        const body = res.getBody();

        bru.setVar("accessToken", body.data.accessToken);
        bru.setVar("refreshToken", body.data.refreshToken);
        console.log("Token runtime:", body.data.accessToken);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
